
import ColorItem from "./ColorItem"
function ColorList({ colors }: { colors?: string[] }) {
    return (
        <div>
            list
            <ColorItem />
        </div>
    )
}

export default ColorList
